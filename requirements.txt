# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.22
matplotlib>=3.5

# RL + environment
gymnasium>=0.28.0

# Optional: if you prefer legacy gym
# gym>=0.21.0


# (Optional) If you intend to use DINO-v2 model utilities that rely on timm or a particular repo:
# timm>=0.6.12

# Notes:
# - For CUDA support, install the correct torch + torchvision wheel from https://pytorch.org
#   e.g. (example) pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# - If you will run on CPU-only, the above torch versions support CPU installs too.
